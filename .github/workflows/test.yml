name: Run Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest
        
        steps:
        
        - name: Install dependencies
          run: |
            make all
        - name: Run tests
          run: pytest test.py